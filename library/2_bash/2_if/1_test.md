Логические операторы.
Логические вычисления в Bash выполняются с помощью утилиты test, или ее псевдонима [...]. Данная утилита принимает сверяемые данные и ключи-операторы и возвращает ноль для истинных выражений и отличное от нуля число в случае ложных выражений.

Логические операторы утилиты test деляться на четыре группы:
1. Операторы И, ИЛИ, НЕ:
  a. [ <выражениеA> -a <выражениеB> ] - оператор И;
  b. [ <выражениеA> -o <выражениеB> ] - оператор ИЛИ;
  c. [ ! <выражениеA> ] - оператор НЕ;
2. Операторы для работы с числами:
  a. [ <числоA> -eq <числоB> ] - "числоA" равно "числуB";
  b. [ <числоA> -ne <числоB> ] - "числоA" не равно "числуB";
  c. [ <числоA> -lt <числоB> ] - "числоA" меньше "числаB";
  d. [ <числоA> -le <числоB> ] - "числоA" меньше или равно "числуB";
  e. [ <числоA> -gt <числоB> ] - "числоA" больше "числаB";
  f. [ <числоA> -ge <числоB> ] - "числоA" больше или равно "числуB".
3. Операторы для работы со строками:
  a. [ <строкаA> = <строкаB> ] - "строкаA" эквивалентна "строкеB";
  b. [ <строкаA> != <строкаB> ] - "строкаA" не эквивалентна "строкеB";
  c. [ <строкаA> > <строкаB> ] - "строкаA" больше "строкиB";
  d. [ <строкаA> >= <строкаB> ] - "строкаA" больше или эквивалентна "строкеB";
  e. [ <строкаA> < <строкаB> ] - "строкаA" меньше "строкиB";
  f. [ <строкаA> <= <строкаB> ] - "строкаA" меньше или эквивалентна "строкеB";
  g. [[ <строкаA> =~ <regex> ]] - "строкаA" соответствует "regex" ("regex" не следует заключать в кавычки);
  h. [ -n <строка> ] - "строка" пуста;
  j. [ -z <строка> ] - "строка" не пуста.
4. Операторы для работы с файловой системой:
  a. [ -e <файл> ] - "файл" существует;
  b. [ -s <файл> ] - "файл" не пуст;
  c. [ -d <файл> ] - "файл" существует и является каталогом;
  d. [ -f <файл> ] - "файл" существует и является обычным файлом;
  e. [ -r <файл> ] - текущий пользователь имеет право читать "файл";
  f. [ -w <файл> ] - текущий пользователь имеет право писать в "файл";
  g. [ -x <файл> ] - текущий пользователь имеет право исполнять "файл";
